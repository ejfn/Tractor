# TypeScript Source Code Review & Clean-up Plan

**Scope**: All 60 `.ts` files in `src/` (~16,250 lines)  
**Goal**: Systematic quality review — correctness, type safety, dead code, naming, duplication, missed edge cases, code style.

---

## Progress Tracker

- [x] **Module 1 — `src/types/`** — *Completed in commit `922e412`*
- [x] **Module 2 — `src/utils/`** — *Completed in commit `5abffc3`*
- [x] **Module 3 — `src/game/`** — *Completed in commit `bdc5ecf`*
- [ ] **Module 4 — `src/hooks/`** — *Next up*
- [ ] **Module 5 — `src/ai/` core**
- [ ] **Module 6 — `src/ai/following/`**
- [ ] **Module 7 — `src/ai/leading/`**
- [ ] **Module 8 — `src/ai/kittySwap/` + `src/ai/trumpDeclaration/`**
- [ ] **Module 9 — `src/ai/llm/`**
- [ ] **Module 10 — `src/locales/`**

---

## Review Findings & Fixes Summary

### Module 1: `src/types/`
- **Deserialization Type Safety**: Replaced unsafe casting in `deserializeCard` with a robust `isCardLike` type guard.
- **AI Game Context Tightening**: Hardened `trumpInfo` to be required in `AIGameContext` rather than optional.
- **Enum & Suit Exclusions**: Constrained `suitVoids` to `Set<PlayableSuit>` (filtering out `Suit.None`) and updated distribution definitions.
- **Callers Refactoring**: Updated all game state initializers and AI managers to conform to the stricter types.

### Module 2: `src/utils/`
- **Card Sorting Type Hardening**: Refactored `getSuitOrderValue` and `getRankOrderValue` to accept exact `Suit`/`Rank` enums rather than broad strings.
- **Joker Suit Logic**: Removed redundant joker ternary branches in `suitHelpers.ts` where both paths returned the identical value.
- **Translation Utility Safety**: Tightened the typing of translation handlers and parameters, replacing generic `any` with strict typing.
- **Fisher-Yates Shuffle Implementation**: Replaced biased `sort(() => 0.5 - Math.random())` in `gameInitialization.ts` with a proper Fisher-Yates array shuffle.
- **Persistence Infinite Retry Bug**: Discovered and resolved a bug in `gameStatePersistence.ts` where calling `getItem` in a retry block passed the static `retries` parameter instead of incrementing it, leading to a possible infinite retry loop.

### Module 3: `src/game/`
- **Card Value Records**: Removed redundant nullish coalescing `|| 0` on exhaustive `Record<Rank, number>` properties.
- **Tractor Logic Type Safety**: Cleaned up suit offsets in `tractorLogic.ts`, transitioning from string lookups to enum values.
- **Multi-Combo Grouping**: Cleaned up unreachable fallback branches in suit grouping functions.
- **Combo Detection Deduplication**: Extracted a duplicated `getLeadingSuit` helper from `playValidation` and `comboDetection` into a single, clean module-level exported function in `comboDetection.ts`.
- **Validation Rules Alignments**: Adapted the rules engine to import unified helpers rather than recreating local equivalents.

---

## Remaining Review Modules & Focus Areas

### Module 4 — `src/hooks/` (6 files, ~1,800 lines)
*React hooks bridging game state and UI*

| File | Purpose / Est. Size |
|------|---------------------|
| `hooks/useGameState.ts` | Game loop state machine integration (~515 lines) |
| `hooks/useGameStatePersistence.ts` | Autocommit state persistence (~396 lines) |
| `hooks/useProgressiveDealing.ts` | Dealing animation coordination (~366 lines) |
| `hooks/useAITurns.ts` | Bot turn queuing and delay logic (~345 lines) |
| `hooks/useAnimations.ts` | React Native reanimated loops (~135 lines) |
| `hooks/useTranslation.ts` | Translations wrapper (~90 lines) |
| `hooks/useTrickResults.ts` | End-of-trick UI metrics (~120 lines) |

- **Focus**: Stale closure bugs in effect callbacks, dependency array completeness, animation loop leaks, and proper coordination of state mutations.

### Module 5 — `src/ai/` core (4 files, ~800 lines)
*Top-level AI strategic orchestration*

| File | Est. Size |
|------|-----------|
| `ai/aiCardMemory.ts` | ~268 lines |
| `ai/aiGameContext.ts` | ~409 lines |
| `ai/aiLogic.ts` | ~200 lines |
| `ai/aiStrategy.ts` | ~80 lines |

- **Focus**: AI context accuracy, player memory completeness, tracking of remaining trump/points, and route selection correctness.

### Module 6 — `src/ai/following/` (10 files, ~2,800 lines)
*Strategic decision pipeline for following trick plays*

- **Focus**: Suit exhaustion rules, team strategy coordination, partner protection logic, and handling of multi-combo/exhausted play conditions.

### Module 7 — `src/ai/leading/` (4 files, ~800 lines)
*Strategic decision pipeline for leading tricks*

- **Focus**: Leading candidates calculation, trick winning probability estimations, point harvesting vs defensive lead balancing.

### Module 8 — `src/ai/kittySwap/` + `src/ai/trumpDeclaration/` (2 files, ~550 lines)
*Pre-round bot actions*

- **Focus**: Declarer kitty swap logic (bot card scoring), declaration bidding strategy, and bidding safety.

### Module 9 — `src/ai/llm/` (6 files, ~2,100 lines)
*LLM bot interface and prompt generation templates*

- **Focus**: Prompt correctness, LLM parser type safety, error boundaries, fallback systems, and token efficiency.

### Module 10 — `src/locales/` (2 files, ~120 lines)
*Translation files configuration*

- **Focus**: Struct type safety and resource configuration correctness.

---

## Review Approach Per Module

For each remaining module:
1. Thoroughly read and analyze all `.ts` files.
2. Catalog any concerns using standard categories: **[BUG]**, **[TYPE]**, **[DEAD]**, **[STYLE]**, **[PERF]**, **[DUPE]**.
3. Formulate proposed fixes and run local validation tests.
4. Obtain user approval before applying changes and committing.
